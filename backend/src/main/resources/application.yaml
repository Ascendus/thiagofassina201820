spring:
  application:
    name: api

  datasource:
    url: jdbc:postgresql://localhost:5432/seplag
    username: seplag
    password: seplag123

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration